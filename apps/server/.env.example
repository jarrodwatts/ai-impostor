# --- realtime server env (Railway) ---
PORT=8080
WS_ALLOWED_ORIGINS=http://localhost:3000

# data stores (Railway-internal URLs in prod)
DATABASE_URL=postgres://user:pass@localhost:5432/ai_impostor
REDIS_URL=redis://localhost:6379

# AI agents
ANTHROPIC_API_KEY=sk-ant-...

# Monad testnet + settlement
MONAD_TESTNET_RPC_URL=https://testnet-rpc.monad.xyz
ESCROW_ADDRESS=0x0000000000000000000000000000000000000000
GAME_MANAGER_PRIVATE_KEY=0x...   # hot wallet: createGame/lockGame/abortGame/settle txs
SERVER_SIGNER_KEY=0x...          # EIP712 settlement signer (split from sender per plan)
